@model IEnumerable<Resturant_RES_MVC_ITI_PRJ.Areas.Client.Models.DishIngredientRel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dish)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredtient)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dish.DishName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ingredtient.IngName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DishIngredientRelId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DishIngredientRelId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DishIngredientRelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
